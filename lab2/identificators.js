// Identificators

// Common variable
let myName = "Oleg";

// Constant variable
const myBirthDate = 2008;

// Function that greets with the single argument
const hello = (name) => {
  console.log(`Hello, ${name}!`);
};

hello(myName);
